{
  "name": "ayurtrace-regulator-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && esbuild index.tsx --bundle --format=esm --outfile=dist/bundle.js --jsx=automatic --external:react --external:react-dom/* --external:recharts --external:jspdf --external:jspdf-autotable --external:leaflet --external:@google/genai && cp index.html dist/index.html && sed -i \"s,%%API_KEY%%,$API_KEY,g\" dist/index.html"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5"
  }
}